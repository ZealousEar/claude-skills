# Copy this file to provider-keys.env and fill in the keys you need.
#
# Claude models: no key needed (uses native Claude Code Task tool)
# ChatGPT models: no key needed if using Codex CLI route (uses your ChatGPT login)
# All models: can route through OpenRouter with a single key

# OpenRouter — one key for all models (openai, google, anthropic, etc.)
OPENROUTER_API_KEY=

# Direct provider keys (only needed if routing directly, not through OpenRouter)
GOOGLE_API_KEY=
MOONSHOT_API_KEY=
