{
  "mathpix": {
    "env_file": ".credentials/dissertation-research/.env",
    "vars": {
      "app_id": "MATHPIX_APP_ID",
      "app_key": "MATHPIX_APP_KEY"
    }
  },
  "ssrn": {
    "cookie_file": ".credentials/cookies/ssrn-cookies.txt",
    "notes": "Cookies expire fast: __cf_bm ~30min, SSRN_TOKEN ~24hr. Re-export from browser (papers.ssrn.com) immediately before use."
  },
  "youtube": {
    "notes": "yt-dlp may need Brave browser cookies for bot detection: yt-dlp --cookies-from-browser brave"
  },
  "vault_root": "$VAULT_ROOT or current working directory",
  "resolve_note": "All paths are relative to vault_root unless absolute. Set VAULT_ROOT env var to your vault path."
}
